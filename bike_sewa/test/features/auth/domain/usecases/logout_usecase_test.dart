import 'package:flutter_test/flutter_test.dart';

void main() {
  group('LogoutUsecase', () {
    test('should clear local storage on logout', () {
      expect(true, true);
    });
    test('should revoke token on server', () {
      expect(true, true);
    });
  });
}
