import 'package:flutter_test/flutter_test.dart';

void main() {
  group('GetCurrentUserUsecase', () {
    test('should return cached user when available', () {
      expect(true, true);
    });
    test('should fetch fresh user from repository', () {
      expect(true, true);
    });
  });
}
