import 'package:flutter_test/flutter_test.dart';

void main() {
  group('RegisterUsecase', () {
    test('should create user on valid params', () {
      expect(true, true);
    });
    test('should throw on duplicate email', () {
      expect(true, true);
    });
  });
}
