import 'package:flutter_test/flutter_test.dart';

void main() {
  group('AuthRepositoryImpl', () {
    test('should login via remote datasource', () {
      expect(true, true);
    });
    test('should cache user locally after login', () {
      expect(true, true);
    });
    test('should logout via remote datasource', () {
      expect(true, true);
    });
  });
}
