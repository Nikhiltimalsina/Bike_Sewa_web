import 'package:flutter_test/flutter_test.dart';

void main() {
  group('BookingRepositoryImpl', () {
    test('should fetch bookings from remote datasource', () {
      expect(true, true);
    });
    test('should cache bookings locally', () {
      expect(true, true);
    });
  });
}
