import 'package:flutter_test/flutter_test.dart';

void main() {
  group('CancelBookingUseCase', () {
    test('should cancel existing booking', () {
      expect(true, true);
    });
    test('should throw when booking is already cancelled', () {
      expect(true, true);
    });
  });
}
