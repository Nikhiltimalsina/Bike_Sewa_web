import 'package:flutter_test/flutter_test.dart';

void main() {
  group('CreateBookingUseCase', () {
    test('should create booking with valid params', () {
      expect(true, true);
    });
    test('should throw when bike is unavailable', () {
      expect(true, true);
    });
  });
}
