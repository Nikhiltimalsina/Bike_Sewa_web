import 'package:flutter_test/flutter_test.dart';

void main() {
  group('GetBookingByIdUseCase', () {
    test('should return booking by id', () {
      expect(true, true);
    });
    test('should throw when booking not found', () {
      expect(true, true);
    });
  });
}
