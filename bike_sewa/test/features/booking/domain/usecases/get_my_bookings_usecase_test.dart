import 'package:flutter_test/flutter_test.dart';

void main() {
  group('GetMyBookingsUseCase', () {
    test('should return list of bookings for current user', () {
      expect(true, true);
    });
    test('should throw on network error', () {
      expect(true, true);
    });
  });
}
