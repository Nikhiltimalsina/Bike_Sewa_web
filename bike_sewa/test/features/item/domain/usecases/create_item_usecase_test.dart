import 'package:flutter_test/flutter_test.dart';

void main() {
  group('CreateItemUsecase', () {
    test('should create item with valid params', () {
      expect(true, true);
    });
    test('should throw on invalid category', () {
      expect(true, true);
    });
  });
}
