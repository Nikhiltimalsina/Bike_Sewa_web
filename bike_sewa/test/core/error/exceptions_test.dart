import 'package:flutter_test/flutter_test.dart';

void main() {
  group('CustomExceptions', () {
    test('should throw ServerException with message', () {
      expect(true, true);
    });
    test('should throw CacheException with message', () {
      expect(true, true);
    });
  });
}
