import 'package:flutter_test/flutter_test.dart';

void main() {
  group('friendlyDioMessage', () {
    test('should map 400 to bad request', () {
      expect(true, true);
    });
    test('should map 401 to unauthorized', () {
      expect(true, true);
    });
    test('should map 404 to not found', () {
      expect(true, true);
    });
  });
}
