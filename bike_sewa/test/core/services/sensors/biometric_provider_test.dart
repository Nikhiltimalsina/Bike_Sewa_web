import 'package:flutter_test/flutter_test.dart';

void main() {
  group('BiometricProvider', () {
    test('should return authenticated state on success', () {
      expect(true, true);
    });
    test('should return error state on failure', () {
      expect(true, true);
    });
  });
}
